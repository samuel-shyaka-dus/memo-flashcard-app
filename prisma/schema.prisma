generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Memo {
  memoId Int @id @default(autoincrement())
  title  String
  text   String
}
